<h1>Hello, <%=current_user.name%></h1>

<h2>User Accounts: </h2>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to "User: #{user.name}", "/users/#{user.id}" %></li>
  <% end %>
  <%= link_to 'User comments', controller: 'articles' %>
</ul>
